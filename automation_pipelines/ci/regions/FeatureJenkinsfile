@Library(['Shared1', 'Shared2'])
import main.groovy.module.*

def module
def firstScript

pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        echo 'Building..'
        stress()
        helloworld()
        script {
          module = load "ci/src/main/groovy/script/StaticScript.groovy"
          firstScript = new FirstScript()
          echo firstScript.a()
          echo SecondScript().a()
          echo SecondScript().test
        }
      }
    }
    stage('Test') {
      steps {
        echo 'Testing..'
        script {
          echo module.getStaticString()
        }
      }
    }
  }
}