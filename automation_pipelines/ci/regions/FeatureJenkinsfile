@Library(['Shared1', 'Shared2'])
import main.groovy.module.*

def module
def firstScript
def secondScript

pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        echo 'Building..'
        stress()
        helloworld()
        script {
          module = load "ci/src/main/groovy/script/StaticScript.groovy"
          firstScript = new FirstScript()
          secondScript = new SecondScript()
          echo firstScript.a()
          echo secondScript.a()
          echo secondScript.test
        }
      }
    }
    stage('Test') {
      steps {
        echo 'Testing..'
        script {
          echo module.getStaticString()
        }
      }
    }
  }
}