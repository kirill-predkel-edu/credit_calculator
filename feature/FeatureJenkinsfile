@Library("Shared1") l1
@Library("Shared2") l2
def module
def a

pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        echo 'Building..'
        stress()
        script {
          module = load "ci/src/main/groovy/script/StaticScript.groovy"
          a = helloWorld()
          println(a)
        }
      }
    }
    stage('Test') {
      steps {
        echo 'Testing..'
        script {
          echo module.getStaticString()
        }
      }
    }
  }
}