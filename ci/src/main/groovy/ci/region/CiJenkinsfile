package ci.region

def bunchOfScripts
def live

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                script {
                    live = new ci.region.StaticScript()
                    echo live.getStaticString()
                    bunchOfScripts = load "ci/src/main/groovy/script/BunchOfScripts.groovy"
                }
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
                script {
                    //echo bunchOfScripts.getProperty('FirstClass').a() не разрешено
                }
            }
        }
    }
}