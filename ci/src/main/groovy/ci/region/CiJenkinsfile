package ci.region

def module

pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        echo 'Building..'
        module = load "ci/src/main/groovy/script/StaticScript.groovy"
      }
    }
    stage('Test') {
      steps {
        echo 'Testing..'
        script {
          echo module.name
        }
      }
    }
  }
}